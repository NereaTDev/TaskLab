
[21:25] TaskLab - Equipo / categorías
- Corregida la lógica del tablero de Equipo para que soporte múltiples asignaciones por CategoryType: ahora cada UserCategoryAssignment se refleja como una mini-card, de modo que los clones (multi-pertenencia) se ven en todas las columnas/subcategorías correspondientes.
- Añadido menú de tres puntos en cada mini-card del tablero de categorías con acciones:
  - "Clonar": marca temporalmente a ese usuario en Alpine (cloneUserId) para que el siguiente drag añada una asignación en vez de mover.
  - "Eliminar": cuando el usuario tiene más de una asignación de ese tipo, permite borrar solo esa asignación concreta (delete_single) sin tocar el resto.
- Implementado feedback visual claro de modo clon:
  - La mini-card del usuario en modo clon se resalta con borde/fondo en tasklab.accent.
  - El botón de tres puntos de esa card también cambia a estado activo (color de acento y fondo oscuro), indicando que el clon está listo para usarse.
- A nivel backend, TeamController@reassignCategory ahora distingue entre:
  - mover (borra todas las asignaciones del tipo y crea una nueva),
  - clon (firstOrCreate sin borrar otras),
  - delete_single (borra solo la asignación concreta a un CategoryValue cuando hay varias).
